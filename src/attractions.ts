
export const get = async (id: string) => {
  // TODO: pull in json until database is added
  return {};
};

/**
 * Reloads cached data.
 */
export const list = async () => {
  const attractions = [];
  return attractions;
};
