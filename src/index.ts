
export const parks = {
  findById: () => {
    // use model
    return {
      /**
       * Retrieve the hours for all parks
       */
      hours: () => {
        // 
      },
      /**
       * retrieve the wait times for all parks
       */
      waitTimes: () => {}
    };
  },
  get: () => {
    // model
  },
  list: () => {
    // model
  },
};
